<UserControl x:Class="ElectionSystem.Views.ReadVotersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ElectionSystem.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <TextBlock Text="{Binding Title}"/>
        </Grid>
        <StackPanel Grid.Row="1" Orientation="Horizontal">
            <Button Content="{StaticResource RegisterVoter}" Command="{Binding CreateCommand}"/>
            <Button Content="{StaticResource LoadVoters}" Command="{Binding ReadCommand}"/>
            <TextBox Width="200" Text="{Binding SearchTerm, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
            <Button Content="{StaticResource SearchVoters}" Command="{Binding SearchCommand}"/>
        </StackPanel>
        <Grid Grid.Row="2">
            <DataGrid ItemsSource="{Binding Collection, UpdateSourceTrigger=PropertyChanged}" AutoGenerateColumns="False" CanUserAddRows="False" CanUserDeleteRows="False" CanUserReorderColumns="False" CanUserResizeRows="False">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="First Name" Binding="{Binding Model.FirstName}"/>
                    <DataGridTextColumn Header="Middle Name" Binding="{Binding Model.MiddleName}"/>
                    <DataGridTextColumn Header="Last Name" Binding="{Binding Model.LastName}"/>
                    <DataGridTextColumn Header="Birthdate" Binding="{Binding Model.Birthdate}"/>
                    <DataGridTextColumn Header="Gender" Binding="{Binding Model.Gender}"/>
                    <DataGridTextColumn Header="Address" Binding="{Binding Model.Address}"/>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
    </Grid>
</UserControl>
