<UserControl x:Class="ElectionSystem.Views.WorkspaceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ElectionSystem.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Border CornerRadius="10" Margin="10" Padding="10" Background="{StaticResource WetAsphalt}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="225"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid Grid.Column="0">
                    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                        <StackPanel>
                            <RadioButton Margin="0,10,0,0" Content="Voters" Command="{Binding GotoVotersCommand}" Style="{StaticResource Menu}"/>
                            <RadioButton Content="Parties" Style="{StaticResource Menu}"/>
                            <RadioButton Content="Elections" Style="{StaticResource Menu}"/>
                            <RadioButton Content="Positions" Style="{StaticResource Menu}"/>
                            <RadioButton Content="Candidacies" Style="{StaticResource Menu}"/>
                            <RadioButton Content="Status" Style="{StaticResource Menu}"/>
                            <RadioButton Content="Users" Style="{StaticResource Menu}"/>
                        </StackPanel>
                    </ScrollViewer>
                </Grid>
                <Grid Grid.Column="1">
                    <Border>
                        <ScrollViewer HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto">
                            <ContentControl Content="{Binding CurrentPage, UpdateSourceTrigger=PropertyChanged}"/>
                        </ScrollViewer>
                    </Border>
                </Grid>
                <Grid Grid.Column="1">
                </Grid>
            </Grid>
        </Border>
    </Grid>
</UserControl>
